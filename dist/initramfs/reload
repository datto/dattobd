#!/bin/sh

ls -l /etc/assurio/dla/mnt/etc/assurio/dla/reload_* > /dev/null 2>&1

if [ "$?" = "0" ]; then
	FILES="/etc/assurio/dla/mnt/etc/assurio/dla/reload_*"
else
	exit
fi

for f in $FILES
do
	$f
done
